<form method="post" action="{{ path('app_finding_delete', {'id': finding.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ finding.id) }}">
    <button class="btn">Delete</button>
</form>
