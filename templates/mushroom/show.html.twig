{% extends 'base.html.twig' %}

{% block title %}Mushroom{% endblock %}

{% block body %}
    <h1>Mushroom</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mushroom.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ mushroom.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mushroom_index') }}">back to list</a>

    <a href="{{ path('app_mushroom_edit', {'id': mushroom.id}) }}">edit</a>

    {{ include('mushroom/_delete_form.html.twig') }}
{% endblock %}
