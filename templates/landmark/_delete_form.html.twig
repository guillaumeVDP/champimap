<form method="post" action="{{ path('app_landmark_delete', {'id': landmark.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ landmark.id) }}">
    <button class="btn">Delete</button>
</form>
